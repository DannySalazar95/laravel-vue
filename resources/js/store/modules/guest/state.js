import mutations from './mutations'
import getters from './getters'

const state = {
    data: {
        first_name:'****',
        last_name:'****'
    },
}

export default {
    state,
    mutations,
    getters
}